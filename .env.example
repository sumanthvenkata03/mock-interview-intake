# Copy to .env for local `vercel dev`, and set the same as Environment Variables in Vercel.
# These are read by the /api/submit serverless function (never commit real values).
GMAIL_USER=webnexa.mockinterviews@gmail.com
GMAIL_APP_PASSWORD=your-16-char-gmail-app-password
TO_EMAIL=webnexa.mockinterviews@gmail.com
